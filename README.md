# RAG-Chatbot-Project

<ins>**Objective**</ins>

The objective of the project is to develop a RAG Chatbot which utilizes RAG(Retrieval Augemented Generation) by extracting data and feeding it into the AI Model and storing the data in a vector database. 
